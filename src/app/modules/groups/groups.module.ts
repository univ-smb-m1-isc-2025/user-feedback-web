import { NgModule } from '@angular/core';

import { GroupUserListModule } from './user-list/user-list.module';

@NgModule({
  imports: [
    // autres imports
    GroupUserListModule,
  ],
})
export class GroupsModule {}
